/**
 * API host to Phoenix application CMS/Backend.
 */
export const BASE_URL = "/api";

/**
 * API endpoints for respective resources.
 */
export const API_ENDPOINT_DESIGNERS = "/designers";
// Note that the API endpoint for toggling fav status is currently found
// within the `updateDesignerFav` ActionCreator.

